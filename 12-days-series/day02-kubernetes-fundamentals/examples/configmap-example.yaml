apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: example
data:
  DATABASE_URL: "postgresql://localhost:5432/mydb"
  LOG_LEVEL: "INFO"
  MAX_CONNECTIONS: "100"
  FEATURE_FLAGS: |
    {
      "new_ui": true,
      "beta_features": false
    }
